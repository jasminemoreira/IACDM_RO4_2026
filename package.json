{
  "name": "hanzi-pwa-w2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.6" },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts"
  },
  "devDependencies": { "typescript": "^7.0.2" }
}
