# Matriz de cobertura — crítica adversarial

## Iteração 1 — V(1)

| id | module | lens | severity | description |
|------|--------|------|----------|-------------|
| ASS-01 | progress | Assumptions | 🔴 | O replay parte de INITIAL e reaplica o log inteiro do cartão, logo assume que o log é COMPLETO desde a criação do cartão. Nada distingue "log completo" de "log parcial": um dispositivo que instale a app depois, ou que sofra evicção de IndexedDB (A-6 admite evicção!) e depois sincronize, replica a partir de um prefixo faltante e produz {n,EF,I} diferente do outro lado. A convergência de C-5 vale para o CONJUNTO de eventos, não para o estado derivado de um conjunto truncado |
| ASS-02 | session | Assumptions | 🟡 | "hoje" é derivado do relógio local do dispositivo no momento da consulta, mas A-10 só protege o localDay GRAVADO. Um usuário que viaja para leste, ou que corrige o relógio, muda o que está devido sem nenhuma revisão ter ocorrido. Nenhuma premissa declara isso |
| ASS-03 | deck | Assumptions | 🟡 | A-9 afirma que o deck cabe em memória mas nenhum limite superior é declarado nem validado; validateDeck aceita um array de tamanho arbitrário e projectAll o percorre inteiro a cada montagem de fila |
| ASS-04 | ui | Assumptions | 🟡 | A-11 exige durabilidade antes de avançar mas assume implicitamente que o append SEMPRE tem sucesso. Não há comportamento definido para falha de escrita (cota estourada, modo privado), e a UI não tem estado para representá-la |
| ASS-05 | service-worker | Assumptions | 🟡 | Assume que o deck.json está no precache. Se o arquivo não for versionado por hash, uma estratégia Cache First o serve indefinidamente e a versão nova nunca chega à aplicação |
| ARQ-01 | storage | Architectural | 🟢 | A dependência de deck e progress está anotada como "só tipos", mas nada no contrato exige `import type` — uma importação de valor entraria sem quebrar nada visível e inverteria a direção da dependência |
| ARQ-02 | session | Architectural | 🟡 | session depende de 4 módulos e concentra decisões que não são dele: quem calcula "hoje", quem guarda a projeção em memória e por quanto tempo. A fronteira com progress não está traçada — é o candidato natural a virar módulo-Deus |
| ARQ-03 | sync | Architectural | 🟡 | O módulo carrega DUAS naturezas no mesmo diretório de 1º nível: reconcile é núcleo puro (C-6) e LocalDoubleTransport é adaptador. Isso enfraquece a alegação de que o núcleo é testável sem navegador, porque o ponto de entrada do módulo expõe os dois |
| ARQ-04 | migration | Architectural | 🟡 | O upgrade de esquema do IndexedDB pertence a quem ABRE o banco (storage, no callback upgrade do idb), mas a tabela atribui essa responsabilidade a migration. Dois donos para a versão de esquema |
| ARQ-05 | ui | Architectural | 🟢 | mount(root, session) não tem porta de renderização, então testar a UI exige DOM real. Aceitável para 3 telas, mas é acoplamento a navegador num módulo que a arquitetura não isola |
| IMP-01 | service-worker | Implementability | 🟡 | A coluna "interface" contém uma configuração de build, não um contrato. Faltam: lista de precache, regra de cache POR TIPO de ativo, política de atualização (skipWaiting ou prompt) e o que acontece com uma versão antiga ainda controlando abas abertas. Não é implementável a partir do documento |
| IMP-02 | sync | Implementability | 🟡 | reconcile está especificada (união por eventId) mas o PROCEDIMENTO de sync não: o que é empurrado (tudo ou delta), se as revisões remotas são gravadas localmente, se a operação é atômica, e a forma de SyncReport nunca é definida |
| IMP-03 | migration | Implementability | 🟡 | DeckDiff e MigrationReport aparecem nas assinaturas sem nenhuma definição de forma. Um implementador teria de inventá-las, e Fase 6 não teria contra o que testar |
| IMP-04 | ui | Implementability | 🟡 | SessionApi e SessionView são referenciados e nunca definidos; as 3 telas estão em prosa, sem máquina de estados. O estado "revelado" (depois de mostrar hanzi, antes de notar) não existe em contrato nenhum |
| IMP-05 | storage | Implementability | 🟢 | Object stores, keyPath e índices não estão especificados — byCard(id) exige um índice em cardId que ninguém declarou |
| SCI-01 | scheduler | Scientific | 🟢 | INITIAL = {n:0, ef:2.5, i:0}: o ef 2.5 tem fonte (Wozniak), o i:0 não — o original só define I(1)=1 APÓS a primeira repetição. É invenção inofensiva mas não declarada |
| SCI-02 | progress | Scientific | 🟢 | A ordem canônica (at, deviceId, eventId) é invenção deste desenho, sem referência bibliográfica. É o que sustenta o determinismo de C-4 e merece ser justificada como decisão de projeto, não apresentada como se derivasse do CRDT (a propriedade de G-Set cobre a UNIÃO, não a ordem do replay) |
| SEC-01 | ui | Security | 🟡 | hanzi, pinyin e gloss vêm de JSON e vão para o DOM. Se a inserção usar innerHTML, um deck adulterado — ou um campo vindo pelo transporte — executa script. Nada no contrato de ui exige textContent |
| SEC-02 | sync | Security | 🔴 | reconcile aceita revisões remotas sem NENHUMA validação de forma ou de faixa. Um remoto adulterado injeta q=99 ou at=Infinity; a fórmula da §4 com q=99 explode o EF e o log é append-only — o dano é permanente e irreversível, porque não existe caminho para remover um evento |
| SEC-03 | storage | Security | 🟢 | O progresso fica em claro no IndexedDB, acessível a qualquer script da mesma origem. Sem dado sensível (§3 exclui autenticação), risco aceito — mas não declarado |
| SEC-04 | service-worker | Security | 🟢 | SW no root controla toda a origem; num host compartilhado (ex.: usuario.github.io) isso alcança outras aplicações do mesmo domínio |
| PERF-01 | progress | Performance | 🟡 | Não há política declarada de quando a projeção é computada nem por quanto tempo vale. Se for a cada render, é O(total de revisões) por quadro; se for cacheada, o cache vira estado derivado vivo que C-3 não regula |
| PERF-02 | sync | Performance | 🟡 | Transport.pull() devolve TODO o log remoto a cada sincronização — o custo cresce linearmente com o histórico para sempre, mesmo quando nada mudou. Não há marca-d'água nem sync incremental |
| PERF-03 | storage | Performance | 🟡 | all() carrega o log inteiro em memória para cada projeção. Combinado com crescimento ilimitado (A-7 não medida), é o gargalo assintótico do sistema |
| REG-01 | session | Regulatory | 🟡 | §7 de REQUISITOS.md (higiene de versionamento: commit por passo com significado próprio) é requisito normativo do projeto e NÃO tem rastreabilidade para módulo nem mecanismo algum na arquitetura |
| REG-02 | service-worker | Regulatory | 🟡 | §6 Inc.2 exige "estratégia de cache declarada por escrito" e nenhum artefato foi designado para conter essa declaração. Sem dono, o critério de aceitação não tem onde ser verificado |
| REG-03 | service-worker | Regulatory | 🟢 | Instalabilidade de PWA exige manifest com name, icons 192/512, start_url e display; o módulo cita "manifest" sem enumerar os campos obrigatórios |
| RES-01 | deck | Resilience | 🔴 | Partida a frio offline depende do precache ter COMPLETADO. Se o usuário fecha a aba durante a primeira visita, a app fica instalada com cache parcial e loadDeck falha sem fallback — o critério VAL-2.2 quebra num estado do qual não há caminho de recuperação definido |
| RES-02 | storage | Resilience | 🟡 | IndexedDB pode lançar QuotaExceededError, estar bloqueado em modo privado ou ser evictado. Nenhum caminho de degradação definido: a aplicação simplesmente quebra |
| RES-03 | sync | Resilience | 🟡 | Se pull() falha no meio, ou se push() tem sucesso e a gravação local falha, os dois lados divergem sem que ninguém saiba. A operação não é declarada atômica nem idempotente ponta a ponta |
| RES-04 | storage | Resilience | 🟡 | Abrir o IndexedDB com esquema novo enquanto outra aba segura o antigo dispara o evento `blocked` do idb. Multi-aba é cenário comum e não tem tratamento |
| RES-05 | session | Resilience | 🟡 | Duas abas abertas: ambas anexam revisões e cada uma mantém projeção em memória desatualizada em relação à outra. Não há invalidação entre abas |
| UX-01 | ui | UI/UX | 🟡 | Não há retorno visual para o intervalo entre tocar a nota e a revisão ficar durável (A-11). O usuário fica sem saber se o toque valeu |
| UX-02 | ui | UI/UX | 🟡 | EDGE-1 (nenhum cartão devido) é uma tela sem saída: não informa QUANDO o próximo cartão vence, então o usuário não sabe se volta em uma hora ou em seis dias |
| UX-03 | ui | UI/UX | 🟡 | Nenhuma especificação de acessibilidade: operação por teclado (revelar e notar), gestão de foco entre cartões, rótulos para leitor de tela, e `lang="zh-Hans"` no hanzi — sem isso o leitor de tela pronuncia caracteres chineses com voz e fonética erradas |
| UX-04 | ui | UI/UX | 🟡 | EDGE-7 (clique duplo) está nomeado nos critérios mas o contrato de ui não tem desabilitação do botão durante a escrita — duas revisões distintas seriam anexadas, e a deduplicação por eventId NÃO as pega porque os eventIds são diferentes |
| UX-05 | ui | UI/UX | 🟢 | Sem indicador de progresso (x de N) na sessão, o usuário não consegue estimar quanto falta para "concluir do início ao fim" (§6 Inc.1) |
| UX-06 | sync | UI/UX | 🟡 | A sincronização é operação visível ao usuário mas nenhum módulo é dono do GATILHO: manual por botão, automática ao reconectar, ou ao abrir? Sem isso não há como descrever o fluxo ao usuário |
| MIG-01 | service-worker | Migration / Coexistence | 🔴 | Interação entre dois módulos: o SW precacheia o deck.json e a app lê a versão de dentro dele. Com Cache First sobre um nome de arquivo não versionado, a v2 nunca chega à aplicação — a migração do §6 Inc.2 jamais dispara e o critério falha silenciosamente, parecendo que "não havia nada a migrar". duplica: ASS-05 (a premissa não declarada); aqui o achado é a falha concreta de aceitação |
| MIG-02 | migration | Migration / Coexistence | 🟡 | O caminho de rollback v2→v1 não está definido: a aplicação aceita uma versão de deck MENOR que a registrada nos metadados, ou trata como corrupção? A decisão de P0 preserva o progresso, mas o comportamento do módulo diante do retrocesso não |
| MIG-03 | storage | Migration / Coexistence | 🟡 | Um upgrade de esquema do IndexedDB que falhe no meio deixa o banco num estado inutilizável sem caminho de recuperação nem de reconstrução a partir do log |
| MIG-04 | migration | Migration / Coexistence | 🟡 | A-2 assume estabilidade do id, mas nada DETECTA sua violação: se o autor do deck reaproveitar "c001" para outra palavra, todo o progresso é silenciosamente transferido para um cartão diferente. O usuário vê um cartão novo já "dominado" |
| SUS-01 | storage | Sustainability / Proportionality | 🟡 | O log cresce para sempre e nunca é compactado — o escopo negativo proíbe snapshot conscientemente, mas nenhum limiar foi definido a partir do qual essa decisão precise ser reexaminada. A 10× (500 cartões, anos de uso) armazenamento e replay crescem juntos sem teto |
| SUS-02 | sync | Sustainability / Proportionality | 🟢 | Transferir o log inteiro a cada sincronização multiplica o custo pelo comprimento do histórico. duplica: PERF-02 |
| PROC-01 | sync | Process / Workflow | 🔴 | O fluxo de sincronização não tem estados nem gatilho definidos, e nada especifica o que acontece se o usuário REVISAR durante uma sincronização em curso. Uma revisão anexada entre o pull e a gravação da união pode ser sobrescrita ou duplicada — exatamente a perda que §6 Inc.3 proíbe |
| PROC-02 | session | Process / Workflow | 🟡 | Os estados da sessão (fila montada → cartão exibido → revelado → notado → próximo → fim) existem só em prosa. Sem máquina de estados, transições órfãs (revelar duas vezes, notar sem revelar) não têm comportamento definido |
| PROC-03 | migration | Process / Workflow | 🟡 | O gatilho da migração é indefinido: quem chama applyDeckVersion e quando — na abertura da app, na ativação do SW, na primeira sessão? Sem dono, pode nunca rodar ou rodar concorrentemente com uma sessão |
| PROC-04 | ui | Process / Workflow | 🟡 | Nenhum caminho de exceção na interface: deck malformado, escrita falhada, sincronização falhada e banco bloqueado não têm tela nem mensagem |
| GOV-01 | sync | Governance / Accountability | 🔴 | deviceId sustenta o desempate de C-4 e a atribuição de cada revisão, mas nada define como é gerado, onde é guardado nem se sobrevive à evicção do IndexedDB (que A-6 admite). Se ele se regenera, o MESMO conjunto de eventos passa a ter ordem de replay diferente nos dois lados — a convergência determinística que é a razão de ser do Incremento 3 deixa de valer |
| GOV-02 | storage | Governance / Accountability | 🟡 | O usuário é dono do próprio progresso mas não há caminho documentado para apagá-lo ou reiniciá-lo, e o log é append-only por contrato — sem uma operação de destruição explícita, o dado é imortal por acidente e não por decisão |
| GOV-03 | progress | Governance / Accountability | 🟢 | A revisão é atribuível a um dispositivo mas não a uma versão da aplicação; um bug de agendamento numa versão não pode ser rastreado até os eventos que ele produziu |
| OBS-01 | sync | Observability / Operability | 🟡 | SyncReport existe na assinatura mas nada o persiste nem o exibe. §6 Inc.3 exige comportamento reproduzível sob conflito — sem registro do que a fusão fez, uma análise posterior não consegue dizer por que o estado ficou como ficou |
| OBS-02 | service-worker | Observability / Operability | 🟡 | Não há como saber qual versão do SW está ativa nem se o precache concluiu. A falha que quebra a partida a frio offline é justamente a invisível |
| OBS-03 | storage | Observability / Operability | 🟡 | Nenhuma forma de inspecionar cota ou detectar evicção. A-6 admite explicitamente que a durabilidade não é garantida, mas o sistema não percebe quando ela falha — o usuário descobre pelo progresso sumido |
| CTRL-01 | scheduler | Control Engineering | 🟡 | O SM-2 é um controlador realimentado (q é o sinal de erro, EF o ganho). Com D1 — EF decaindo também em q<3, divergência consciente do original — um cartão repetidamente falhado leva o EF ao piso 1,3 e fica preso ali: a recuperação é de +0,1 por acerto perfeito, contra quedas de até −0,8 por falha. É windup assimétrico, consequência documentada mas não monitorada |
| CTRL-02 | session | Control Engineering | 🟡 | Não há regulação do TAMANHO da fila. Quem passa um mês sem revisar recebe o deck inteiro devido de uma vez (avalanche), e "concluir a sessão do início ao fim" (§6 Inc.1) vira inatingível na prática. O desenho não define comportamento algum para esse regime |
| LING-01 | progress | Linguistics / Grammar | 🔴 | `at: number` admite duas leituras corretas e incompatíveis — epoch em milissegundos ou em segundos. Duas implementações do mesmo contrato (ou um transporte HTTP futuro trocando JSON com este) ordenariam e datariam eventos de formas divergentes, e o erro só apareceria como intervalos absurdos muito depois. O mesmo vale para DayStr, cujo formato está no comentário e não no tipo |
| LING-02 | sync | Linguistics / Grammar | 🟡 | A união é por eventId, mas a GERAÇÃO do eventId não é especificada. Se dois dispositivos produzirem o mesmo id para revisões diferentes, a união DESCARTA uma delas em silêncio — violando "nenhum lado perde revisões" pelo próprio mecanismo que deveria garanti-lo |
| LING-03 | deck | Linguistics / Grammar | 🟡 | "lança em malformado" não diz o que é malformado: ids duplicados no deck, campos vazios, pinyin sem tom, version ausente. Duas implementações corretas de validateDeck aceitariam decks diferentes |
| LING-04 | scheduler | Linguistics / Grammar | 🟢 | O campo `i` de SchedState não carrega a unidade no tipo; "intervalo" em dias e "intervalo" em milissegundos são confundíveis na fronteira com nextDueDay |
| MEC-01 | service-worker | Mechanical Engineering | 🟡 | Nenhuma versão de dependência está fixada na arquitetura. O comportamento de precache e de atualização do vite-plugin-pwa varia entre versões maiores do Workbox, e é dele que VAL-2.2 depende — uma atualização de rotina pode quebrar o critério sem tocar em código do projeto |
| MEC-02 | service-worker | Mechanical Engineering | 🟡 | "o navegador" é tratado como uma coisa só. Safari/iOS aplica evicção após 7 dias sem uso e tem ciclo de vida de SW distinto; sem declarar quais navegadores devem passar VAL-2.2, a tolerância do sistema à variação de ambiente é desconhecida |
| MEC-03 | scheduler | Mechanical Engineering | 🟡 | EF é ponto flutuante acumulado por somas sucessivas: dois caminhos de cálculo matematicamente iguais podem divergir no último bit. Qualquer comparação de estado entre dispositivos por igualdade exata falharia, e nenhuma tolerância está declarada |
| MEC-04 | progress | Mechanical Engineering | 🟢 | Uma revisão duplicada localmente (dois eventos distintos para o mesmo toque) não é removível por nenhum mecanismo: a deduplicação da união opera por eventId e esses são diferentes. relacionado: UX-04 |

## Iteração 2 — V(3)

> Nota de rastreabilidade: o contador de versão do motor está em V(3); a seção
> correspondente em `specs/technical/architecture.md` é a intitulada **V(2)** —
> o desenho de 8 módulos produzido pela Fase 3 da iteração 1. É esse o alvo
> criticado aqui.

| id | module | lens | severity | description |
|------|--------|------|----------|-------------|
| LING-05 | sync | Linguistics / Grammar | 🔴 | `Transport.push(rs: Review[])` não diz se o remoto SUBSTITUI seu log pelo enviado ou o UNE. Se substituir: o dispositivo A faz pull, B empurra eventos novos, A empurra o log que capturou antes — e apaga do remoto os eventos de B. A perda que a §6 Inc.3 proíbe volta pela porta do transporte, exatamente onde a rejeição do LWW não olhava. Duas implementações corretas do contrato divergem no essencial |
| RES-06 | sync | Resilience | 🔴 | O duplo local vive na MESMA origem e no mesmo IndexedDB do log primário. C-8 afirma que a perda total é reparada pela união com o remoto — mas a evicção, que A-6 declara ser por origem e total, destrói os dois ao mesmo tempo. O mecanismo de reparo compartilha o domínio de falha daquilo que repara |
| ASS-06 | progress | Assumptions | 🔴 | C-9 manda `parseReview` rejeitar evento cujo `cardId` não esteja no deck. Um cartão REMOVIDO na v2 do deck tem revisões históricas legítimas: rejeitá-las apaga o progresso que a decisão de P0 mandou preservar ("removidos preservam progresso inativo, nunca apagado"). Contrato novo contradiz decisão de escopo anterior |
| LING-06 | session | Linguistics / Grammar | 🟡 | Contradição interna de V(2): a tabela de módulos declara `grade(cardId, q) -> Promise<Result<void, WriteError>>`, e o bloco de tipos declara `SessionApi.grade(q) -> Promise<void>`. Assinatura e aridade diferentes para a mesma operação |
| ARQ-06 | progress | Architectural | 🟡 | Depois de absorver o núcleo de `sync`, `progress` reúne três preocupações distintas: validar entrada externa (`parseReview`), álgebra de conjunto/ordem (`union`, `canonicalOrder`) e projeção de domínio (`projectCard`). A validação de entrada externa é de natureza diferente das outras duas — ARQ-03 foi corrigido movendo o problema, não dissolvendo-o |
| ARQ-07 | storage | Architectural | 🟡 | `storage` acumulou versão de esquema, cota E geração/guarda do `deviceId`. Identidade do dispositivo não é persistência: é o único dado que o módulo CRIA em vez de guardar |
| UX-07 | ui | UI/UX | 🟡 | `syncNow()` só é alcançável nas telas `done` e `queue-empty`. Combinado com a avalanche aceita em CTRL-02, um usuário que volta após um mês nunca chega a `done` — e portanto **nunca consegue sincronizar**, que é justamente quando mais precisa. Duas decisões individualmente defensáveis se anulam |
| PERF-04 | session | Performance | 🟡 | C-14 manda recomputar `projectAll` a cada nota registrada. Numa sessão de 50 cartões com o log no limiar declarado de A-7 (50 000 eventos), são 50 varreduras completas do log numa única sessão — o limiar de A-7 e a política de C-14 foram fixados sem se olharem |
| SEC-05 | progress | Security | 🟡 | C-9 valida `atEpochMs` como "finito e positivo", sem teto. Um evento remoto datado de 2099 produz `dueDay` no futuro distante e some com o cartão para sempre — negação de serviço silenciosa sobre um cartão específico, sem nada inválido no dado |
| SCI-03 | scheduler | Scientific | 🟡 | `nextDueDay(day: LocalDay, i: Days)` é aritmética de CALENDÁRIO, não de milissegundos: somar `i * 86 400 000` erra em transições de horário de verão, e o Brasil já teve e pode voltar a ter DST. Nada no contrato diz qual das duas aritméticas vale |
| IMP-06 | service-worker | Implementability | 🟡 | Os sinais `offline-ready` e `sw-version` são prometidos sem mecanismo: `postMessage` para os clientes, evento customizado, ou consulta ativa da app? Sem isso o módulo continua não implementável a partir do documento — IMP-01 foi parcialmente corrigido |
| OBS-04 | storage | Observability / Operability | 🟡 | `estimateQuota()` foi exposta para resolver OBS-03, mas nenhum módulo a chama e nenhum limiar diz o que fazer com o número. Capacidade sem consumidor não é observabilidade — é API morta |
| GOV-04 | service-worker | Governance / Accountability | 🟡 | C-13 exige que `cache-strategy.md` fique em sincronia com a configuração do módulo, e nada verifica isso. Um documento normativo que pode divergir em silêncio do sistema que descreve é pior que a ausência dele, porque a auditoria confia nele |
| PROC-05 | session | Process / Workflow | 🟡 | O estado `writing` não tem saída por tempo: se a gravação no IndexedDB nunca resolver (aba bloqueada, transação pendurada), a sessão fica presa com os botões inertes e sem caminho para `error`. Estado terminal não intencional |
| MIG-05 | deck | Migration / Coexistence | 🟡 | `diffDecks(prev, next)` precisa do deck ANTERIOR, mas C-11 troca o deck no precache a cada build e a app carrega sempre o novo. O anterior só existe se `DeckRepository` o tiver guardado — o que a tabela não exige nem em contrato nem em momento definido |
| MEC-05 | service-worker | Mechanical Engineering | 🟡 | A-14 fixa as dependências diretas com `~`, mas o Workbox que importa é o TRANSITIVO, puxado pelo `vite-plugin-pwa` — e é dele que depende o comportamento de precache que sustenta VAL-2.2. A tolerância declarada não cobre a dependência que realmente varia |
| SUS-03 | sync | Sustainability / Proportionality | 🟡 | Aceitar a troca do log íntegro (PERF-02) como mecanismo de reparo tem custo que ninguém limitou: no limiar de A-7 são 50 000 eventos empurrados a cada sincronização, mesmo quando nada mudou. O limiar foi definido para o replay, não para o transporte |
| CTRL-03 | scheduler | Control Engineering | 🟢 | A-16 afirma resultado bit-idêntico entre dispositivos, mas isso só vale quando ambos têm o MESMO conjunto de eventos — ou seja, pós-sincronização. A premissa não diz isso, e lida isoladamente promete mais do que C-8 permite |
| REG-04 | session | Regulatory | 🟢 | `commit-protocol.md` deu dono ao §7 mas nenhum mecanismo o verifica; a conformidade depende inteiramente de disciplina na Fase 5 |
| GOV-05 | storage | Governance / Accountability | 🟢 | O caminho de apagamento documentado para GOV-02 é "limpar dados do site", que destrói também o duplo local. relacionado: RES-06 — o único caminho de reinício é o mesmo que elimina o backup |
| MEC-06 | progress | Mechanical Engineering | 🟢 | `canonicalOrder` desempata por `deviceId` em ordem lexicográfica de UUID; UUIDs v4 não têm ordem semântica, então o desempate é estável mas arbitrário. Correto para convergência, porém não corresponde a nenhuma ordem temporal real |
